export const environment = {
  production: true,

  // In production, calls go through the same host and Nginx proxies /api to backend.
  apiUrl: 'https://ecotrack-office-mitn.onrender.com/api/v1',
};
